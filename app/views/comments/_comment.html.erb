<div>
	<p><%= comment.name %></p>
	<p><%= comment.body %></p>
	<p><%= time_ago_in_words(comment.created_at) %></p>

	<%= link_to 'Delete',[comment.pelicula,comment], method: :Delete, data: {comfirm: 'Are you sure?'} %>

</div>	
	
